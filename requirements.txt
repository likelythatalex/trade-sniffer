# Pinned for reproducibility (local venv == CI). Bump deliberately, then re-test.
# Runtime
yfinance==1.4.1         # market data (daily + native weekly)
pandas==3.0.3           # OHLCV frames + rolling features
numpy==2.4.6            # numerics under pandas
PyYAML==6.0.3           # config.yaml
Jinja2==3.1.6           # HTML dashboard template
requests==2.34.2        # Discord webhook
pandas-market-calendars==5.4.0  # NYSE trading calendar for missing-bar detection
vaderSentiment==3.3.2   # news-sentiment strategy (deterministic lexicon scorer; pluggable)

# Dev / test
pytest==9.1.0
lxml==6.1.1             # only for scripts/build_universe.py (Wikipedia table parsing)
